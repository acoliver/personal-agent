# Phase 02 Completion: Model & Trait Changes

## Status: COMPLETE

## Changes Made

### New Files
- `src/models/search.rs` — `SearchResult`, `SearchMatchType`
- `src/models/context_state.rs` — `ContextState`

### Modified Files
- `src/models/conversation.rs` — Added `ConversationMetadata` struct; extended `Message` with `model_id`, `tool_calls`, `tool_results` fields (all `Option<String>`, `#[serde(default)]`); updated all Message constructors (`user`, `assistant`, `assistant_with_thinking`, `system`) to initialize new fields as `None`
- `src/models/mod.rs` — Exports `ConversationMetadata`, `ContextState`, `SearchMatchType`, `SearchResult`
- `src/services/conversation.rs` — Replaced `list`, `add_user_message`, `add_assistant_message` with `list_metadata`, `add_message`, `search`, `message_count`, `update_context_state`, `get_context_state`; added required imports
- `src/services/conversation_impl.rs` — Implemented all new trait methods on JSON-based `ConversationServiceImpl`: `list_metadata` (maps `Conversation` → `ConversationMetadata`), `add_message` (unified), `search` (returns `Ok(Vec::new())` — correct behavior for JSON backend), `message_count`, `update_context_state` (no-op), `get_context_state` (returns `None`)

## Placeholder Scan
Zero occurrences of `unimplemented!()`, `todo!()`, or `// TODO:` in any changed file.

## Build Verification

`cargo check --lib` produces **7 errors** in exactly the expected Phase 04 downstream files:

**src/presentation/chat_presenter.rs** (5 errors):
- E0599: no method `list` (line 299)
- E0282: type annotations needed (line 301)
- E0599: no method `list` (line 401)
- E0282: type annotations needed (line 401)
- E0282: type annotations needed (line 408)

**src/services/chat_impl.rs** (2 errors):
- E0599: no method `add_user_message` (line 144)
- E0599: no method `add_assistant_message` (line 437)

**Zero errors in Phase 02 changed files:**
- `src/models/conversation.rs` — [OK] no errors
- `src/models/search.rs` — [OK] no errors
- `src/models/context_state.rs` — [OK] no errors
- `src/models/mod.rs` — [OK] no errors
- `src/services/conversation.rs` — [OK] no errors (the `conversation.rs:46` reference in output is a compiler `help:` note, not an error)
- `src/services/conversation_impl.rs` — [OK] no errors

## Trait Method Verification

ConversationService trait now contains exactly (per spec §2.1):

**Unchanged:** `create`, `load`, `rename`, `delete`, `set_active`, `get_active`, `get_messages`, `update`

**Replaced:**
- `list(limit, offset) → Vec<Conversation>` → `list_metadata(limit, offset) → Vec<ConversationMetadata>`
- `add_user_message(id, content) → Message` } → `add_message(id, message) → Message`
- `add_assistant_message(id, content, thinking) → Message` }

**New:** `search(query, limit, offset) → Vec<SearchResult>`, `message_count(id) → usize`, `update_context_state(id, &ContextState) → ()`, `get_context_state(id) → Option<ContextState>`

**Removed:** `list`, `add_user_message`, `add_assistant_message`

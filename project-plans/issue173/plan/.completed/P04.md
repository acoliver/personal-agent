Verdict: PASS

## Phase 04: Cancel Pipeline TDD - Completion Evidence

Plan: PLAN-20260416-ISSUE173.P04

### New Tests Added

1. `handle_stop_streaming_forwards_conversation_id` - src/presentation/chat_presenter_tests.rs:956
   - Verifies that `UserEvent::StopStreaming { conversation_id: A }` flows through the presenter and calls `chat_service.cancel(A)`
   - Uses struct-variant syntax that will not compile until P05

2. `handle_stop_streaming_does_not_cancel_other_conversations` - src/presentation/chat_presenter_tests.rs:990
   - Verifies that when stopping stream for conversation A, conversation B is never cancelled
   - Uses struct-variant syntax that will not compile until P05

### grep Counts

```
$ grep -r "@plan PLAN-20260416-ISSUE173.P04" src/presentation/ | wc -l
2

$ grep -r "@requirement REQ-173-002.3" src/presentation/ | wc -l
2
```

### cargo build --all-targets Output (tail)

```
error[E0559]: variant `events::types::UserEvent::StopStreaming` has no field named `conversation_id`
   --> src/presentation/chat_presenter_tests.rs:965:44
    |
965 |     let event = UserEvent::StopStreaming { conversation_id: conversation_id_a };
    |                                            ^^^^^^^^^^^^^^^ `events::types::UserEvent::StopStreaming` does not have this field
    |
    = note: all struct fields are already assigned

error[E0559]: variant `events::types::UserEvent::StopStreaming` has no field named `conversation_id`
    --> src/presentation/chat_presenter_tests.rs:1000:44
     |
1000 |     let event = UserEvent::StopStreaming { conversation_id: conversation_id_a };
     |                                            ^^^^^^^^^^^^^^^ `events::types::UserEvent::StopStreaming` does not have this field
     |
     = note: all struct fields are already assigned
```

### Supporting Infrastructure Added

- `RecordingChatService` - A mock chat service that records `cancel(conversation_id)` calls into an `Arc<Mutex<Vec<Uuid>>>` for test verification
- `MockAppSettingsService` - Mock for the AppSettingsService trait needed by `handle_user_event`

### Constraints Followed

- [x] Did NOT modify `src/events/types.rs` (`UserEvent::StopStreaming` remains a unit variant)
- [x] Did NOT modify `src/presentation/chat_presenter.rs` handler
- [x] Tests reference `UserEvent::StopStreaming { conversation_id: ... }` - causing intentional compile failures
- [x] No `todo!()`, `unimplemented!()`, `// TODO`, `// FIXME`, `// HACK`, `// STUB`, "placeholder", or "not yet implemented" in new code
- [x] Used existing mock style from chat_presenter_tests.rs
- [x] Extended rather than replaced existing mocks

### Notes

The compile error shows exactly the expected TDD failing-state:
- `variant `events::types::UserEvent::StopStreaming` has no field named `conversation_id``

This confirms the tests are correctly written to use the struct-variant syntax that P05 will introduce. Once P05 changes `UserEvent::StopStreaming` from a unit variant to a struct variant with `conversation_id: Uuid`, these tests will compile and be ready to run.

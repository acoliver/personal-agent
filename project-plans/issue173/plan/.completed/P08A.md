# Phase 08a — Store Set TDD Verification (Re-Verification After Remediation)

**Plan:** `PLAN-20260416-ISSUE173.P08a`  
**Date:** 2026-04-18  
**Previous Verdict:** FAIL (tests 1 and 2 asserted against disconnected `AppStoreInner::default()`)  
**Remediation:** Added `#[cfg(test)]` accessor `GpuiAppStore::active_streaming_targets_for_test()`, rewrote tests 1 and 2 to use it.

---

## Mandatory Checks

### Check 1: File exists

```
$ ls -la src/ui_gpui/app_store_streaming/tests_concurrent.rs
-rw-r--r--@ 1 acoliver  staff  7832 Apr 17 22:50 src/ui_gpui/app_store_streaming/tests_concurrent.rs
```

[OK] PASS — file exists, 7832 bytes.

---

### Check 2: Plan marker count (>=4 total required)

```
$ grep -c "@plan PLAN-20260416-ISSUE173.P08" src/ui_gpui/app_store_streaming/tests_concurrent.rs src/ui_gpui/app_store.rs
src/ui_gpui/app_store_streaming/tests_concurrent.rs:4
src/ui_gpui/app_store.rs:1
```

[OK] PASS — 5 total (4 + 1), threshold is >=4.

---

### Check 3: Requirement marker count (>=3 required)

```
$ grep -c "@requirement REQ-173-004" src/ui_gpui/app_store_streaming/tests_concurrent.rs
3
```

[OK] PASS — 3 matches, threshold is >=3.

---

### Check 4: Module wiring

```
$ grep -n "mod tests_concurrent" src/ui_gpui/app_store_streaming.rs src/ui_gpui/app_store_streaming/tests.rs
src/ui_gpui/app_store_streaming.rs:207:mod tests_concurrent;
```

[OK] PASS — module wired at line 207 in app_store_streaming.rs.

---

### Check 5: No disconnected `AppStoreInner::default()` (EXPECT EMPTY)

```
$ grep -n "AppStoreInner::default()" src/ui_gpui/app_store_streaming/tests_concurrent.rs
(no output)
```

[OK] PASS — no disconnected defaults remain.

---

### Check 6: Accessor usage locations

```
$ grep -n "active_streaming_targets_for_test" src/ui_gpui/app_store.rs src/ui_gpui/app_store_streaming/tests_concurrent.rs
src/ui_gpui/app_store.rs:844:    pub(crate) fn active_streaming_targets_for_test(&self) -> std::collections::HashSet<Uuid> {
src/ui_gpui/app_store_streaming/tests_concurrent.rs:105:    let targets = store.active_streaming_targets_for_test();
src/ui_gpui/app_store_streaming/tests_concurrent.rs:144:    let targets = store.active_streaming_targets_for_test();
src/ui_gpui/app_store_streaming/tests_concurrent.rs:155:    let targets = store.active_streaming_targets_for_test();
```

[OK] PASS — accessor defined once in app_store.rs (L844, inside `#[cfg(test)] impl GpuiAppStore`), used 3 times in tests (L105 in test 1, L144 and L155 in test 2).

---

### Check 7: All 3 test functions present

```
$ grep -n "multiple_targets_can_be_tracked_concurrently\|finalize_removes_only_targeted_conversation_from_set\|streaming_state_snapshot_active_flag_reflects_set_membership" src/ui_gpui/app_store_streaming/tests_concurrent.rs
79:fn multiple_targets_can_be_tracked_concurrently() {
119:fn finalize_removes_only_targeted_conversation_from_set() {
169:fn streaming_state_snapshot_active_flag_reflects_set_membership() {
```

[OK] PASS — all 3 tests present at lines 79, 119, 169.

---

### Check 8: Cargo build fails with TDD RED signal

```
$ cargo build --all-targets 2>&1 | tail -25
error[E0308]: mismatched types
   --> src/ui_gpui/app_store_streaming/tests_concurrent.rs:222:9
    |
219 |     let snapshot_for_c = project_streaming_snapshot(
    |                          -------------------------- arguments to this function are incorrect
...
222 |         &active_set,
    |         ^^^^^^^^^^^ expected `Option<Uuid>`, found `&HashSet<Uuid>`
    |
    = note:   expected enum `std::option::Option<uuid::Uuid>`
            found reference `&HashSet<uuid::Uuid>`
note: function defined here
   --> src/ui_gpui/app_store_types.rs:101:8
    |
101 | pub fn project_streaming_snapshot<S: BuildHasher>(
    |        ^^^^^^^^^^^^^^^^^^^^^^^^^^
...
104 |     active_streaming_target: Option<Uuid>,
    |     -------------------------------------

Some errors have detailed explanations: E0308, E0609.
For more information about an error, try `rustc --explain E0308`.
error: could not compile `personal_agent` (lib test) due to 3 previous errors
warning: build failed, waiting for other jobs to finish...

$ cargo build --all-targets 2>&1 | grep -E "^error"
error[E0609]: no field `active_streaming_targets` on type `std::sync::MutexGuard<'_, AppStoreInner>`
error[E0308]: mismatched types
error[E0308]: mismatched types
error: could not compile `personal_agent` (lib test) due to 3 previous errors
```

[OK] PASS — TDD RED confirmed. Three compile errors:
1. **E0609** — `no field active_streaming_targets` on `AppStoreInner` (accessor in app_store.rs:848)
2. **E0308** — `project_streaming_snapshot` arg 3 expects `Option<Uuid>`, got `&HashSet<Uuid>` (tests_concurrent.rs:210)
3. **E0308** — same mismatch at tests_concurrent.rs:222

All errors are the expected TDD RED signals that P09 will resolve.

---

### Check 9: Production field not yet added (EXPECT EMPTY)

```
$ grep -n "active_streaming_targets:" src/ui_gpui/app_store.rs
(no output)
```

[OK] PASS — `active_streaming_targets:` (plural, HashSet field) does not exist in production code yet.

---

### Check 10: Forbidden patterns

```
$ grep -n 'unimplemented!\|todo!\|// TODO\|// FIXME\|// HACK\|// STUB\|placeholder\|not yet implemented' src/ui_gpui/app_store_streaming/tests_concurrent.rs src/ui_gpui/app_store.rs
(no output)
```

[OK] PASS — no forbidden patterns in either file.

---

### Check 11: Detailed Test Logic Review

#### Test 1: `multiple_targets_can_be_tracked_concurrently` (L79–L114)

1. Creates a real `GpuiAppStore::from_startup_inputs(...)` with conversations A and B.
2. Calls `begin_and_ready(&store, conversation_a)` to select and load messages for A.
3. Reduces `ViewCommand::ShowThinking { conversation_id: conversation_a, ... }` to start streaming on A.
4. Calls `begin_and_ready(&store, conversation_b)` to select and load messages for B.
5. Reduces `ViewCommand::ShowThinking { conversation_id: conversation_b, ... }` to start streaming on B.
6. Calls `store.active_streaming_targets_for_test()` → returns `HashSet<Uuid>`.
7. Asserts `targets.contains(&conversation_a)` and `targets.contains(&conversation_b)`.

[OK] CORRECT — Uses real store accessor. No disconnected `AppStoreInner::default()`.

#### Test 2: `finalize_removes_only_targeted_conversation_from_set` (L119–L164)

1. Creates a real `GpuiAppStore` with conversations A and B.
2. Starts streaming on A via `begin_and_ready` + `AppendStream`.
3. Starts streaming on B via `begin_and_ready` + `AppendStream`.
4. At L144: `let targets = store.active_streaming_targets_for_test();` — asserts both A and B are in the set.
5. Reduces `ViewCommand::FinalizeStream { conversation_id: conversation_a, tokens: 42 }` to finalize only A.
6. At L155: `let targets = store.active_streaming_targets_for_test();` — asserts A is NOT in the set, B IS still in the set.

[OK] CORRECT — All assertions via the real store API accessor. No `AppStoreInner::default()`.

#### Test 3: `streaming_state_snapshot_active_flag_reflects_set_membership` (L169–L228)

1. Creates a real store and starts streaming on A and B (for realistic context setup).
2. Manually constructs `active_set: HashSet<Uuid>` with A and B inserted.
3. Manually constructs `state_map: HashMap<Uuid, ConversationStreamingState>` with entries for A and B.
4. Calls `project_streaming_snapshot(&state_map, Some(conversation_a), &active_set)` — asserts `snapshot_for_a.active_target == Some(conversation_a)` (A is selected and in the active set).
5. Calls `project_streaming_snapshot(&state_map, Some(conversation_c), &active_set)` — asserts `snapshot_for_c.active_target == None` (C is selected but NOT in the active set).

[OK] CORRECT — Tests the projection function directly with manual construction. Uses the new `&HashSet<Uuid>` signature (which will compile once P09 changes the function signature).

---

### Check 12: git diff --stat confirms scoped changes

```
$ git diff --stat src/ui_gpui/
 src/ui_gpui/app_store.rs              | 20 ++++++++++++++++++++
 src/ui_gpui/app_store_streaming.rs    |  2 ++
 src/ui_gpui/views/chat_view/render.rs | 16 ++++++++++------
 3 files changed, 32 insertions(+), 6 deletions(-)

$ git status --short src/ui_gpui/app_store_streaming/tests_concurrent.rs
A  src/ui_gpui/app_store_streaming/tests_concurrent.rs
```

[OK] PASS — P08a-specific changes are:
- `app_store.rs` — +20 lines (the `#[cfg(test)] impl GpuiAppStore` accessor block, L834–L851)
- `app_store_streaming.rs` — +2 lines (the `mod tests_concurrent;` wiring at L207)
- `tests_concurrent.rs` — new file (staged, 228 lines)
- `views/chat_view/render.rs` — pre-existing changes from prior phase (out of P08a scope)

No unexpected production code modifications.

---

## Summary

| Check | Description | Result |
|-------|-------------|--------|
| 1 | File exists | [OK] PASS |
| 2 | Plan markers >=4 | [OK] PASS (5) |
| 3 | Requirement markers >=3 | [OK] PASS (3) |
| 4 | Module wiring | [OK] PASS (L207) |
| 5 | No AppStoreInner::default() | [OK] PASS (empty) |
| 6 | Accessor present and used | [OK] PASS (1 def + 3 uses) |
| 7 | All 3 test functions present | [OK] PASS |
| 8 | Cargo build TDD RED | [OK] PASS (E0609 + 2×E0308) |
| 9 | Production field not added | [OK] PASS (empty) |
| 10 | No forbidden patterns | [OK] PASS (empty) |
| 11 | Test logic review | [OK] PASS (all 3 correct) |
| 12 | Scoped git changes | [OK] PASS |

## Blocking Issues

None.

## Verdict

**PASS** — All 12 mandatory checks pass. The remediation successfully eliminated the disconnected `AppStoreInner::default()` pattern from tests 1 and 2. Tests now correctly assert against real store state via the `#[cfg(test)]` accessor `active_streaming_targets_for_test()`. Test 3 correctly tests the projection function directly with manual construction. TDD RED state is maintained — all compile errors are the expected signals for P09 implementation.

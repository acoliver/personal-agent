## Verdict: PASS

## Summary

Added two TDD (test-first) behavioral tests to `src/llm/client_agent/tests.rs` that verify the planned `resolve_all_for_conversation` method:

1. **resolve_all_for_conversation_resolves_only_target** (line 252) - @plan PLAN-20260416-ISSUE173.P06, @requirement REQ-173-003.2
   - Verifies that `resolve_all_for_conversation(conv_a, false)` only resolves approvals for conversation A, not conversation B
   - Asserts the returned vec contains exactly one entry with the correct conversation UUID and request ID
   - Verifies waiter_a resolves with `false`, then explicitly resolves waiter_b with `true`

2. **resolving_one_conversation_does_not_wake_another** (line 276) - @plan PLAN-20260416-ISSUE173.P06, @requirement REQ-173-003.1
   - Creates two waiters for two different conversations
   - Uses `_waiter_a` binding (not `let _ =`) to prevent premature Drop
   - Calls `resolve_all_for_conversation(conv_a, false)` and verifies waiter_b is still pending via timeout

## Verification Results

### grep counts
- `@plan PLAN-20260416-ISSUE173.P06`: 2 (expected >= 2) [OK]
- `@requirement REQ-173-003`: 2 (expected >= 2) [OK]
- `resolve_all_for_conversation`: 3 (expected >= 2) [OK]

### cargo build tail (compile errors as expected for TDD)
```
error[E0599]: no method named `resolve_all_for_conversation` found for struct `client_agent::ApprovalGate` in the current scope
   --> src/llm/client_agent/tests.rs:271:25
    |
271 |     let resolved = gate.resolve_all_for_conversation(conv_a, false);
    |                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^ method not found in `client_agent::ApprovalGate`
    |
   ::: src/llm/client_agent.rs:26:1
    |
 26 | pub struct ApprovalGate {
    | ----------------------- method `resolve_all_for_conversation` not found for this struct

error[E0599]: no method named `resolve_all_for_conversation` found for struct `client_agent::ApprovalGate` in the current scope
   --> src/llm/client_agent/tests.rs:293:18
    |
293 |     let _ = gate.resolve_all_for_conversation(conv_a, false);
    |                  ^^^^^^^^^^^^^^^^^^^^^^^^^^^^ method not found in `client_agent::ApprovalGate`
    |
   ::: src/llm/client_agent.rs:26:1
    |
 26 | pub struct ApprovalGate {
    | ----------------------- method `resolve_all_for_conversation` not found for this struct

error: could not compile `personal_agent` (lib test) due to 2 previous errors
```

### placeholder grep output
- No matches found (exit code 1, meaning no forbidden patterns detected) [OK]

## Plan marker count

Tests carry the plan marker as specified:
- Line 251: `/// @plan PLAN-20260416-ISSUE173.P06`
- Line 275: `/// @plan PLAN-20260416-ISSUE173.P06`

Total: 2 markers (all new tests tagged)

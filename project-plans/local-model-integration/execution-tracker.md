# Execution Tracker — PLAN-20260903-LOCALMODEL

| Phase | ID | Status | Started | Completed | Verified | Semantic? | Notes |
|-------|-----|--------|---------|-----------|----------|-----------|-------|
| 0.5 | P05A | [x] | 2026-09-03 | 2026-09-03 | [x] | N/A | Preflight: deps resolve, llama.cpp compiles (Metal) |
| 01 | P01 | [x] | 2026-09-03 | 2026-09-03 | [x] | [x] | Settings svc + seeding; local_model_settings_tests 6/6, profile_seeding_tests 8/8 |
| 02 | P02 | [x] | 2026-09-03 | 2026-09-04 | [x] | [x] | Engine actor + Model impl + client seam; local_engine_tests 8 pass + 1 ignored |
| 03 | P03 | [x] | 2026-09-03 | 2026-09-04 | [x] | [x] | Profile editor honesty + invalidations; legacy-id fix in profile_migration.rs |
| 04 | P04 | [x] | 2026-09-04 | 2026-09-04 | [x] | [x] | Settings UI Local Model section; gpui_wiring_local_model_tests 11/11, lib settings 101 pass, app_store 81 pass; fmt+clippy clean |
| 05 | P05 | [x] | 2026-09-04 | 2026-09-04 | [x] | [x] | P05 slice: (2026-09-04): EngineStatus::Loaded gained total_layers (#[serde(default)], layers = requested gpu layers clamped to n_layer) + card renders "Metal: N/M layers · ctx N · last gen N tok/s" (plain "N layers" when total unknown); profile dropdown local rows show engine-status dot from a snapshot captured in toggle_profile_dropdown (no poll loop); manual run for Andrew pending |
| R1 | UX-REMEDIATION | [x] | 2026-09-04 | 2026-09-04 | [x] | [x] | After Andrew's first manual run: (1) P0 routing bug — get_serdes_provider laundered "local"→"openai" (effective_serdes_provider fallback arm), chat + title gen hit api.openai.com 401; fixed with a guard returning LOCAL_PROVIDER_ID before registry/quirks resolution, 4 regression tests drive the real seam (client_tests + llm_client_helpers model_spec pin). (2) Missing-GGUF error now carries "Pick the GGUF file in Settings → Local Model". (3) Profile editor Local variant: engine status row + shared GGUF path + Choose… + free-text model_id. (4) Settings gains "Create 'Granite (local)' profile" (ensure_local_seed_profile, makes default) when none exists. (5) Precedence pinned: persisted path > PA_LOCAL_GGUF env (env is first-run default only). Logs tmp/verify-localmodel-b/ |
| R2 | EMPTY-STREAM-FIX | [x] | 2026-09-04 | 2026-09-04 | [x] | [x] | Root cause: LocalLlamaModel::request_stream dropped Generation (and its AbortGuard) after moving events into GenerationStream; the guard's Drop inserted the gen_id into the engine cancel set, so the actor stopped before sampling the first token → clean Complete, zero content, on every chat + title request. Fix: stream owns the guard via Generation::into_parts, released on normal completion so a post-stream drop can't write a stale cancel entry (src/llm/local/llama_model.rs, @requirement:REQ-LM-004). Repro hardware test tests/local_model_repro_tests.rs (#[ignore], prints prompt/events; before: input_tokens=398 output_tokens=0, after: visible text). Scripted regression local_engine_tests::request_stream_holds_the_abort_guard_for_the_streams_lifetime. Missed by tests because ScriptedGenerator never consults its cancel set and the raw-prompt hardware test bypasses request_stream. Logs tmp/verify-localmodel-c/ |
| R3 | EXIT-ABORT-FIX | [x] | 2026-09-04 | 2026-09-04 | [x] | [x] | Root cause: process exit ran llama.cpp teardown under a live engine — the detached actor thread still held the backend guard and (after a generation with idle_unload=false) a Metal-backed model/context, so __cxa_finalize's C++ static destructors raced them (SIGABRT, exit 101, flaky; repro test aborted 2/2 pre-fix in tmp/verify-localmodel-d/). atexit-hook shutdown REJECTED with evidence: ggml registers function-local-static destructors lazily at first use (backend init, model load, Metal device) — all AFTER any hook registered inside main — so atexit LIFO destroys them before the hook runs and the hook's own llama_free faulted (SIGBUS in llama_context::~llama_context on the actor thread, crash report 2026-09-04-110708.ips). Fix: no llama.cpp work during finalize, ever — quiesce while fully alive instead: Job::Shutdown makes the actor drop ctx/model/backend on its own stack and exit (engine.rs), a per-engine AtomicBool shares the cancel path so an in-flight generation stops at the next token boundary and the exit-time join is bounded; dropping the LAST EngineHandle runs that shutdown (impl Drop, strong-count check) which covers every test-spawned engine mid-process; app quit paths call local::shutdown_local() explicitly (main_gpui.rs after the GPUI run loop returns; chat_view render_bars exit button before its std::process::exit). Scripted regression engine_shutdown_joins_the_actor_and_fails_new_jobs pins shutdown-joins + fail-fast on later jobs; ENGINE_SPAWN_LOCK serializes engine spawns in local_engine_tests (backend is process-global). Design constraint kept: engine thread + backend guard still live for the whole process during normal operation; shutdown is exit-only, not per-request. Logs tmp/verify-localmodel-d/ |

## Verification Log (tmp/verify-localmodel-a/)
- fmt: exit 0 (2026-09-04)
- clippy --all-targets -D warnings: exit 0 (2026-09-04)
- Full suite `cargo test --lib --tests`: exit 0 — 1978 passed / 0 failed / 34 ignored, 135 binaries
- GGUF hardware test `PA_LOCAL_GGUF=... cargo test --test local_engine_tests -- --ignored --nocapture`: PASS — real_model_generates_and_idle_unloads ok, Metal4 (MTLGPUFamilyMetal4), 7.26s
- P04 (2026-09-04): `cargo check --lib` ok; `cargo test --test gpui_wiring_local_model_tests` 11/11; `cargo test --lib settings` 101 pass (2 cycle_active_field tests updated for the extended tab order); `cargo test --test app_store_tests` 81 pass; `cargo fmt --all -- --check` exit 0; `cargo clippy --all-targets -- -D warnings` exit 0
- P05 final slice (2026-09-04): `cargo check --lib` exit 0; `cargo test --test gpui_wiring_local_model_tests` 12/12 (new: status_payload_without_total_layers_still_parses); `cargo test --test local_engine_tests` 8 pass / 1 ignored; `cargo test --lib -- settings chat_view loaded_detail engine_states profile_dropdown` 259 pass (new: 2 card detail-line tests, dot token-pairing test, dropdown snapshot test; 1 finding fixed en route — headless fallback palette collides text_muted/warning, so the test pins token identity, mac-native keeps them distinct); `cargo fmt --all` exit 0; `cargo clippy --all-targets -- -D warnings` exit 0
- FINAL SWEEP (2026-09-04, final-sweep.log, after all phases): fmt exit 0; clippy exit 0; full suite exit 0 with 1996 passed / 0 failed; GGUF hardware test exit 0 (real_model_generates_and_idle_unloads ok on Metal4)
- R2 (2026-09-04, tmp/verify-localmodel-c/): repro-before.log FAILED exactly as the app log (input_tokens=398, output_tokens=0, single clean StreamComplete); repro-after.log PASS with visible text deltas ("Hello! How can I help…"); local_engine_tests 9 pass + 1 ignored (new abort-guard regression); local_model_settings_tests 7 pass; fmt --check exit 0; clippy --all-targets -D warnings exit 0
- R3 (2026-09-04, tmp/verify-localmodel-d/): pre-fix repro hardware test SIGABRT 2/2 (exit 101 after green assertions; backtrace __cxa_finalize_ranges); mid-attempt atexit-hook variant SIGBUS on the actor thread (crash report local_model_repro_tests-2026-09-04-110708.ips, llama_context dtor) → hook approach rejected. Final design: repro ×2 exit 0, local_engine_tests --ignored ×2 exit 0 (all with PA_LOCAL_GGUF=granite Q8_0); local_engine_tests 10 pass + 1 ignored (new shutdown regression); local_model_settings_tests 7 pass; fmt --check exit 0; clippy --all-targets -D warnings exit 0

## Fixes During Verification
- 57 clippy errors + fmt drift from interrupted P02/P03 run: fixed (mechanical).
- profile_seeding legacy test failure: root cause was parse_modern_legacy_profile discarding stored ids (update wrote new file, stale original untouched); fixed to preserve stored id + update() normalizes local base_url (REQ-LM-007).
- Stale lib tests test_create_and_list_profiles / test_delete_profile contradicted REQ-LM-002 seeding; primed via write_default_id (already-initialized install), assertions kept.

## Completion Markers
- [x] All phases have @plan markers in code (P01-P05)
- [x] All requirements have @requirement markers (implemented ones)
- [x] Verification commands pass — final full sweep green
- [x] No phases skipped
- [ ] Manual run for Andrew: launch personal_agent_gpui, verify fresh-install seeding, first-send load, Settings status card, Unload now, idle unload

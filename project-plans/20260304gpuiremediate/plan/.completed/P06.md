# Phase 06 Evidence: Startup Hydration Convergence

## Phase ID
`PLAN-20260304-GPUIREMEDIATE.P06`

## Verdict: PASS

## Changes Summary

### Structured Startup Inputs (Mode A)
- `src/ui_gpui/app_store.rs`: Added `StartupInputs`, `StartupSelectedConversation`, `StartupMode::ModeA`, `StartupTranscriptResult::Success/Failure`
- `src/ui_gpui/app_store.rs`: Added `pub fn from_startup_inputs(inputs: StartupInputs) -> Self` using `begin_selection(BatchNoPublish)` + `reduce_view_command_without_publish(ConversationMessagesLoaded)` for transcript success, or `ConversationLoadFailed` for transcript failure
- Startup is Mode A: transcript is synchronously known from `conversation_service.get_messages()`

### Legacy Startup Path Removal
- `src/main_gpui.rs`: `build_startup_view_commands()` replaced with `build_startup_inputs()` returning `(StartupInputs, Vec<ViewCommand>)` — structured inputs for store + settings-only commands for legacy settings view compat
- `src/ui_gpui/app_store.rs`: `from_startup_commands()` removed; `reduce_startup_batch_without_publish()` refactored to work with `StartupInputs`
- `src/main_gpui.rs`: Store created via `GpuiAppStore::from_startup_inputs(startup_inputs)` at app startup
- `MainPanelAppState.startup_commands` now carries only settings/profile commands (ShowSettings + ChatProfilesUpdated), NOT chat transcript commands

### MainPanel Startup Application
- `src/ui_gpui/views/main_panel.rs`: `apply_startup_commands()` now applies store snapshot FIRST, then replays only the settings/profile compatibility commands
- Chat/history first-frame state comes entirely from the store snapshot
- Settings views still get their data via `handle_command(ShowSettings/ChatProfilesUpdated)` since they don't consume store snapshots

### Test Updates
- `tests/presenter_selection_and_settings_tests.rs`: `startup_and_manual_selection_converge_on_one_authoritative_delivery_path` now verifies convergence through the store API directly — both startup and runtime use `begin_selection` + `ConversationMessagesLoaded` through the same store reducer
- `tests/chat_startup_scrollback_layout_regression_tests.rs`: `startup_and_runtime_transcript_delivery_converge_on_store_snapshot_mount_path` verifies `build_startup_view_commands` is gone and `build_startup_inputs` exists

## Verification Evidence

### cargo check
```
cargo check --all-targets -q → exit 0, no errors
```

### Test Results
- chat_startup_scrollback_layout_regression_tests: 4 passed, 0 failed
- presenter_selection_and_settings_tests: 5 passed, 0 failed
- gpui_wiring_command_routing_tests: 18 passed, 0 failed
- gpui_wiring_event_flow_tests: 22 passed, 0 failed
- seven_bugs_regression_tests: 14 passed, 0 failed
- chat_view_conversation_switch_regression_tests: 5 passed, 1 expected-red (local transcript clear — Phase 08 scope)

### Legacy Removal
```
grep "build_startup_view_commands" src/main_gpui.rs → no matches
grep "from_startup_commands" src/ui_gpui/app_store.rs → no matches
```

### Placeholder Detection
```
grep "todo!\|unimplemented!" across P06-touched files → no matches
```

### Plan Marker Coverage
- 10 `@plan PLAN-20260304-GPUIREMEDIATE.P06` markers across 4 files

## Startup Mode
**Mode A**: transcript outcome is synchronously available at startup via `conversation_service.get_messages()`. The startup batch uses `begin_selection(conversation_id, BatchNoPublish)` + `ConversationMessagesLoaded { selection_generation: 1 }` to reach `Ready` state before first publication.

## Expected-Red Carry-Forward (Phase 08 scope)
- `manual_selection_still_clears_local_transcript_before_runtime_replay_arrives` — `select_conversation_at_index()` still locally clears transcript in ChatView before store snapshot arrives

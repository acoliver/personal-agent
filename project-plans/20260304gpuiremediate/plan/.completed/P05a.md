# Phase 05a Evidence: Presenter/Store Runtime Integration Verification

## Phase ID
`PLAN-20260304-GPUIREMEDIATE.P05a`

## Verdict: PASS

## Prerequisite Check
```
test -f project-plans/20260304gpuiremediate/plan/.completed/P05.md → exists
grep "Verdict: PASS" → confirmed
```

## Verification Evidence

### cargo check
```
cargo check --all-targets -q → exit 0, no errors
```

### Test Results
- `gpui_wiring_command_routing_tests`: 18 passed, 0 failed
- `gpui_wiring_event_flow_tests`: 22 passed, 0 failed
- `presenter_selection_and_settings_tests`: 4 passed, 1 expected-red (startup convergence — Phase 06 scope)
- `chat_view_conversation_switch_regression_tests`: 5 passed, 1 expected-red (local transcript clear — resolves with store authority)
- `seven_bugs_regression_tests`: 14 passed, 0 failed

### Placeholder Detection
```
grep -rn "todo!\|unimplemented!" across src/main_gpui.rs src/ui_gpui/ src/presentation/ src/events/types.rs → no matches (exit 1)
```

## Structural Verification

### Store reduction path exists for runtime presenter updates
- `spawn_runtime_bridge_pump` at `src/main_gpui.rs:370` drains bridge commands every 16ms and calls `app_store.reduce_batch(commands)`
- `reduce_batch` at `src/ui_gpui/app_store.rs:238` iterates commands through `reduce_view_command_without_publish`

### Selection loading protocol exists in code
- `begin_selection_locked` at `src/ui_gpui/app_store.rs:386` mints generation, sets `Loading` state
- `ConversationMessagesLoaded` reducer at `app_store.rs:470` performs generation-gated bulk replacement, transitions to `Ready`
- `ConversationLoadFailed` reducer at `app_store.rs:502` performs generation-gated error transition

### Chat/history views consume updated snapshot state
- `ChatView::apply_store_snapshot` at `chat_view.rs:346` applies transcript, streaming, load_state, conversations, selected id/title
- `HistoryView::apply_store_snapshot` at `history_view.rs:111` applies conversation list and selected id

### Plan markers
- 25+ `@plan PLAN-20260304-GPUIREMEDIATE.P05` markers across 7 production files
- `@requirement REQ-ARCH-003.*` markers in app_store.rs, selection_intent_channel.rs, main_gpui.rs, main_panel.rs, events/types.rs

### `selection_generation` present on protocol types
- `ViewCommand::ConversationActivated { id, selection_generation }` — confirmed in view_command.rs
- `ViewCommand::ConversationMessagesLoaded { conversation_id, selection_generation, messages }` — confirmed
- `ViewCommand::ConversationLoadFailed { conversation_id, selection_generation, message }` — confirmed
- `UserEvent::SelectConversation { id, selection_generation }` — confirmed in events/types.rs

## Semantic Verification

### Sole production drainer
- `drain_commands()` called only from `src/main_gpui.rs:395` (bridge pump)
- Definition in `gpui_bridge.rs:60` is the only other reference
- `ensure_bridge_polling` in `main_panel.rs:389` is now a no-op

### Sole ordinary-runtime minting site
- `begin_selection` called from production code only via `src/main_gpui.rs:319` (`handle_select_conversation_intent`)
- Other `begin_selection_locked` calls are internal to `app_store.rs` (startup batch reducer)
- Views use `selection_intent_channel().request_select(conversation_id)` — no generation minting

### Same-id reselection semantics
- `begin_selection_locked` at `app_store.rs:391-408`: Loading/Ready with matching id/generation → `NoOpSameSelection`
- Error state falls through to retry at `app_store.rs:404,411` — mints new generation

### ConversationActivated is idempotent echo
- `app_store.rs:447-468`: matching id + generation + already-Loading → return false (no mutation)
- matching id + generation + Ready → only title-provenance upgrade if applicable

### No ordinary transcript clear on ConversationActivated
- `app_store.rs:447-468`: ConversationActivated never touches `inner.snapshot.chat.transcript`

### ConversationMessagesLoaded bulk replacement for matching generation only
- `app_store.rs:470-501`: guards on conversation_id match, selection_generation match, load_state target match; then overwrites transcript

### Stale/off-target payloads ignored
- `ConversationMessagesLoaded`: `app_store.rs:475-483` returns false for mismatched id or generation
- `ConversationLoadFailed`: `app_store.rs:507-510` returns false for mismatched id or generation
- Returning false means no mutation, no revision bump, no publish

### Ignored commands don't bump revision or publish
- `reduce_view_command_without_publish` returns `bool`; `reduce_batch` at `app_store.rs:246` only calls `bump_revision_and_publish` if any command returned `changed = true`

### Selection intent → store boundary → presenter flow
- Views: `selection_intent_channel().request_select(conversation_id)` (chat_view.rs:420, history_view.rs:368)
- Pump: `drain_selection_intents` → `handle_select_conversation_intent` → `begin_selection` → `bridge.emit(UserEvent::SelectConversation { id, selection_generation })` → presenter
- Presenter: `chat_presenter.rs` consumes enriched `selection_generation`, emits generation-aware `ConversationActivated`, `ConversationMessagesLoaded`, `ConversationLoadFailed`

### Non-chat command forwarding preserved
- Pump clones commands before reducing, then `forward_runtime_commands_to_main_panel` pushes the full batch to MainPanel.handle_command for settings/MCP/profile routing

### FinalizeStream durable transcript materialization
- Store reducer handles FinalizeStream with a dedupe guard (`last_finalized_stream_guard`) that prevents duplicate materializations
- Stale/off-target FinalizeStream returns false (no mutation)

### Bounded ConversationCleared
- ChatView.handle_command(ConversationCleared) still handles local clear and can restore from `app_store.restore_after_clear()` in the same synchronous update

## Expected-Red Carry-Forwards (Phase 06/07 scope)
1. `startup_and_manual_selection_do_not_yet_converge_on_one_authoritative_delivery_path` — startup still uses legacy `startup_commands`
2. `manual_selection_still_clears_local_transcript_before_runtime_replay_arrives` — `select_conversation_at_index()` still locally clears

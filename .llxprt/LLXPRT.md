## LLxprt Code Added Memories
- PersonalAgent now has a GPUI-based UI system documented with comprehensive resources including README.md updates, architectural documentation in dev-docs/architecture/gpui-architecture.md, and module-level documentation in src/ui_gpui/README.md to support the development and maintenance of this new UI architecture
- For this gpuui (Rust) project, when asked to handle GitHub issue #NUM: checkout/pull main (no clean/delete), create branch issueNUM, read issue/comments with gh, make a test-first plan, implement with rust-focused workflow, run full verification (cargo fmt --all -- --check, cargo clippy --all-targets -- -D warnings, cargo test --lib --tests, structural/lizard checks, plus coverage/E2E as applicable), then commit/push and create a detailed PR with Fixes #NUM; after PR, watch checks with gh pr checks NUM --watch --interval 300 and loop on CI/CodeRabbit fixes until all checks are green.
- PersonalAgent Linux KDE tray: the root cause of the "no icon shows" bug was TWO issues: (1) ksni's default `tokio` feature caused a zbus panic ("there is no reactor running") that killed the tray worker thread before it could register with the SNI watcher — fixed by switching to `ksni = { version = "0.3", default-features = false, features = ["blocking", "async-io"] }`; (2) the tray used `icon_name()` with a freedesktop named icon fallback which was unreliable — fixed by implementing `icon_pixmap()` with inline ARGB32 pixel data decoded from the embedded PNG via a `LazyLock<ksni::Icon>`. Additionally, popup positioning required `WindowKind::PopUp` (not `Normal`) on Linux to set `override_redirect=true` and bypass KWin's window placement policy which was re-centering the popup. The popup anchors to bottom-right, 52px above the taskbar.
- PersonalAgent ZAI API setup: ZAI coding plan key (e6a4d1418d294fd4a70d32cf8ed6eeac.83RHYdtkxffcmIx9) is stored in Linux Secret Service with attributes service=personal-agent, username=apikey:zai-coding-plan. Profile JSON is at ~/.config/PersonalAgent/profiles/ with provider_id="anthropic", model_id="glm-4.6", base_url="https://api.z.ai/api/coding/paas/v4". The keyring crate must use features=["sync-secret-service"] (NOT "linux-native") on Linux for KDE Wallet/Secret Service to work.
